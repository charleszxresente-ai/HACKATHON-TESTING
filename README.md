Challenge 1: Reverse Greeting Function
Participants must write code inside "solution.py" to reverse a text string and make it uppercase. 
Must commit using a separate branch. 

Challenge 2: Array Sum & Pull Request Review
Participants must write a function that sums only the even numbers in an array.
Must commit using a separate branch. 

Challenge 3: Leaderboard Sorter & Issue
Participants must fix a bug in a broken script so scores sort numerically in descending order instead of as strings.
The commit message must use GitHub syntax EX.(closes #1), so that it will automatically resolve the reported issue. (still figuring out on how to run this one)
